{
  "name": "never-miss-twice",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Offline-first fitness-, voeding- en gewoontetracker. Statische site, geen buildstap.",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
